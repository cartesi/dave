[package]
name = "cartesi-machine-sys"
version = { workspace = true }
edition = { workspace = true }

authors = { workspace = true }
description = { workspace = true }
homepage = { workspace = true }
license-file = { workspace = true }
readme = { workspace = true }
repository = { workspace = true }


[features]
remote_machine = []

# Keeps manifest-level enforcement for packaged consumers: unlike the env-only
# default, enabling this feature fails rather than falling back to source.
external_cartesi = []


[dependencies]
link-cplusplus = "1"


[build-dependencies]
bindgen = "0.72"
sha2 = "0.10"
