linux.bin
rootfs.ext2

**/machine-image/
**/machine-image.fingerprint
**/machine-image.fingerprint.pending
**/machine-image.fingerprint.tmp.*

**/*tar.gz

# Lua machine wrapper snapshot litter (hash-named dirs beside the
# source images); a proper scratch-dir fix is queued.
**/0x*/
